# Copy to .env for local development:
#   cp .env.example .env
#
# Matches docker-compose.yml. Start the database with:
#   docker compose up -d   (from the repository root)

DATABASE_URL="postgresql://policygraph:policygraph@localhost:5432/policygraph"
PORT=3000
# Origin allowed by the API for local development.
CLIENT_URL="http://localhost:5173"
